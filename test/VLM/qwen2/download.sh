#!/usr/bin/env bash
# download_qwen2.5_72b.sh - Скачивание Qwen/Qwen2.5-72B-Instruct
# Основано на точном списке файлов (37 шард_weights + конфиги)

set -euo pipefail

# --- НАСТРОЙКИ ---
MODEL_ID="Qwen/Qwen2.5-72B-Instruct"
# Папка для сохранения (убедитесь, что есть ~150 ГБ места)
TARGET_DIR="/mnt/nvme/huggingface/Qwen2.5-72B-Instruct"

echo "🚀 Скачиваем $MODEL_ID"
echo "📂 Папка назначения: $TARGET_DIR"
echo "⚠️  Модель состоит из 37 частей, общий размер ~145 ГБ."

# Создаем папку
mkdir -p "$TARGET_DIR"
cd "$TARGET_DIR"

# Функция для скачивания с поддержкой докачки
do_download() {
    local url="$1"
    local filename="$2"
    
    echo "📥 Скачиваю: $filename"
    
    # Wget с докачкой (-c), таймаутом и 3 попытками при ошибке
    wget -c --timeout=3600 --tries=3 --continue \
         --user-agent="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36" \
         -O "$filename" "$url" || {
        echo "❌ Ошибка скачивания $filename. Попробуйте запустить скрипт снова."
        return 1
    }
}

# ---------------------------------------------------------
# ЧАСТЬ 1: Скачивание весов (37 файлов safetensors)
# ---------------------------------------------------------
echo "========================================"
echo "⬇️  ЧАСТЬ 1: Веса модели (37 файлов)..."
echo "========================================"

# Цикл от 001 до 037 (Qwen2.5 разбита на 37 частей)
for i in {001..037}; do
    # Формируем имя файла: model-00001-of-00037.safetensors
    FILE_NAME=$(printf "model-%05d-of-00037.safetensors" "$i")
    FILE_URL="https://huggingface.co/$MODEL_ID/resolve/main/$FILE_NAME"
    
    do_download "$FILE_URL" "$FILE_NAME"
done

echo "✅ Часть 1 завершена: Все веса скачаны."

# ---------------------------------------------------------
# ЧАСТЬ 2: Скачивание конфигурационных и служебных файлов
# ---------------------------------------------------------
echo "========================================"
echo "⬇️  ЧАСТЬ 2: Конфигурации и Токенизатор..."
echo "========================================"

# Список всех нужных файлов (полный список из вашего сообщения)
CONFIG_FILES=(
    "config.json"
    "generation_config.json"
    "model.safetensors.index.json"
    "tokenizer.json"
    "tokenizer_config.json"
    "vocab.json"
    "merges.txt"      # Важно для GPT2/BPE токенизатора Qwen
    "README.md"        # Документация (по желанию, но полезно иметь)
    "LICENSE"          # Лицензия
    ".gitattributes"   # Атрибуты git (для полноты)
)

for file in "${CONFIG_FILES[@]}"; do
    FILE_URL="https://huggingface.co/$MODEL_ID/resolve/main/$file"
    do_download "$FILE_URL" "$file"
done

echo "✅ Часть 2 завершена: Конфигурации скачаны."

# ---------------------------------------------------------
# ИТОГИ
# ---------------------------------------------------------
echo ""
echo "========================================"
echo "🎉 СКАЧИВАНИЕ УСПЕШНО ЗАВЕРШЕНО!"
echo "========================================"
echo "📂 Путь: $TARGET_DIR"
echo ""
echo "📊 Проверка скачанных весов (первые 5):"
ls -lh model-*.safetensors | head -5
echo "..."
echo "📊 Проверка скачанных весов (последние 5):"
ls -lh model-*.safetensors | tail -5
echo ""
echo "📋 Все файлы в папке:"
ls -lh