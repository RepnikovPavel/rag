#!/usr/bin/env bash
# download_qwen2_vl_72b.sh - Скачивание Qwen/Qwen2-VL-72B-Instruct

set -euo pipefail

# --- НАСТРОЙКИ ---
MODEL_ID="Qwen/Qwen2-VL-72B-Instruct"
TARGET_DIR="/mnt/nvme/huggingface/Qwen2-VL-72B-Instruct"

echo "🚀 Скачиваем $MODEL_ID"
echo "📂 Папка назначения: $TARGET_DIR"
echo "⚠️  Модель состоит из 38 частей, общий размер ~145 ГБ."

# Создаем папку
mkdir -p "$TARGET_DIR"
cd "$TARGET_DIR"

# Функция для скачивания с поддержкой докачки
do_download() {
    local url="$1"
    local filename="$2"
    
    echo "📥 Скачиваю: $filename"
    
    # Wget с докачкой (-c), таймаутом и 3 попытками
    wget -c --timeout=3600 --tries=3 --continue \
         --user-agent="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36" \
         -O "$filename" "$url" || {
        echo "❌ Ошибка скачивания $filename. Попробуйте запустить скрипт снова."
        return 1
    }
}

# ---------------------------------------------------------
# ЧАСТЬ 1: Скачивание весов (38 файлов safetensors)
# ---------------------------------------------------------
echo "========================================"
echo "⬇️  ЧАСТЬ 1: Веса модели (38 файлов)..."
echo "========================================"

# ИСПРАВЛЕННЫЙ ЦИКЛ: {1..38} вместо {001..038}
for i in {1..38}; do
    # printf форматирует 1 как 00001, 8 как 00008 и т.д.
    FILE_NAME=$(printf "model-%05d-of-00038.safetensors" "$i")
    FILE_URL="https://huggingface.co/$MODEL_ID/resolve/main/$FILE_NAME"
    
    do_download "$FILE_URL" "$FILE_NAME"
done

echo "✅ Часть 1 завершена: Все веса скачаны."

# ---------------------------------------------------------
# ЧАСТЬ 2: Скачивание конфигурационных и служебных файлов
# ---------------------------------------------------------
echo "========================================"
echo "⬇️  ЧАСТЬ 2: Конфигурации, Токенизатор, VL-препроцессинг..."
echo "========================================"

CONFIG_FILES=(
    "config.json"
    "generation_config.json"
    "model.safetensors.index.json"
    "preprocessor_config.json"
    "tokenizer.json"
    "tokenizer_config.json"
    "vocab.json"
    "merges.txt"
    "chat_template.json"
    "README.md"
    "LICENSE"
)

for file in "${CONFIG_FILES[@]}"; do
    FILE_URL="https://huggingface.co/$MODEL_ID/resolve/main/$file"
    do_download "$FILE_URL" "$file"
done

echo "✅ Часть 2 завершена: Конфигурации скачаны."

# ---------------------------------------------------------
# ИТОГИ
# ---------------------------------------------------------
echo ""
echo "========================================"
echo "🎉 СКАЧИВАНИЕ УСПЕШНО ЗАВЕРШЕНО!"
echo "========================================"
echo "📂 Путь: $TARGET_DIR"
echo ""
echo "📊 Проверка скачанных весов (первые 5):"
ls -lh model-*.safetensors | head -5
echo "..."
echo "📊 Проверка скачанных весов (последние 5):"
ls -lh model-*.safetensors | tail -5
echo ""
echo "📋 Все файлы в папке:"
ls -lh